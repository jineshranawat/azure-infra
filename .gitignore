# Local environment — never commit
.env
.venv/

# Cursor IDE local state — never commit
.cursor/
.cursorrules

# Generated cost dashboard output (re-create with cost-dashboard.cmd)
docs/cost-dashboard-out/

# Terraform local state / plugins / saved plans (never commit)
**/.terraform/
**/*.tfstate
**/*.tfstate.*
**/terraform.tfvars
**/.terraform.lock.hcl.backup
**/tfplan.autoguard

# Python caches
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Enterprise notify local runtime (Jira mock / mail outbox / logs)
shared-adf-lab/enterprise-notify/out/
shared-adf-lab/enterprise-notify/jira_mock/data/issues.json
shared-adf-lab/enterprise-notify/jira_mock/data/attachments/
shared-adf-lab/enterprise-notify/mail_sink/data/outbox.json
shared-adf-lab/enterprise-notify/mail_sink/data/eml/

# Enterprise DE playbook local outputs
shared-adf-lab/enterprise-de/out/

# Genie lab local outputs (agent state + question results)
shared-adf-lab/genie-lab/out/

# Nested Foundry Code Repository clone (own .git; clone via Foundry stemma URL)
rtt-transforms/
