"""Shared, unit-tested helper library for the RTT-Programme pipeline.

Every module here is engine-agnostic and deterministic so it can be tested with
pytest fixtures and reused from both Polars (lightweight) and PySpark transforms.
"""
